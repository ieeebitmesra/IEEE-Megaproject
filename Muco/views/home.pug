extends base

block timeline
   .timeline
    // TL Banner
    .tl__banner
      #barShow.menu_bar-icon
        i.fas.fa-bars
      .home_menu_bar.tl__banner-text Home
      #ratingShow.menu_bar-icon
        i.fas.fa-star-half-alt
    // TL posts
    .posts
   

block cityReview 
  include _cityReview
  script(src='./../js/timeline.js' defer)
