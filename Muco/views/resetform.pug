doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Document
  script(src='/js/resetform.js' defer)
  link(rel='stylesheet' href='/css/reset.css')
  link(href='https://fonts.googleapis.com/css2?family=Kanit:wght@800&family=Open+Sans:wght@400;500;600;700;800&family=Readex+Pro:wght@300;400;500;600;700&display=swap' rel='stylesheet')
.alert.hidden
.resetpass
  .logoinresetform
    .contentfor
      //
        <span class="welcome-div banner-highlight"> Welcome to </span>
        <br />
      // Welcome to
      span.banner-highlight.m m
      span.banner-highlight.u u
      span.banner-highlight.c c
      span.banner-highlight.o o
      // <span class="banner-highlight">!</span>
  .reset_2_password(data-token=`${token}`)
    .reset-content
      h4 Create New Password
      p.para-reset-first Your new password must be different from previous used passwords.
      form#form-reset(action='')
        .password-reset
          .newPassword
            div
              label(for='password') New Password
            input(type='password' id='init-pwd' placeholder='New Password')
          .confirmPassword
            div
              label(for='confirm password') Confirm Password
            input(type='password' id='re-pwd' placeholder='Confirm Password')
        button.btn-reset Reset
      // <p class="Team_para_verify">Team Muco.</p>


